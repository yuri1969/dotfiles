# Install dependencies defined by the given file:
pip install -r 'requirements.txt'

# Write pip dependency resolution actions to 'file.json':
pip install -r 'requirements.txt' --dry-run --report '/path/to/file.json'
